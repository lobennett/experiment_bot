__pycache__/
*.pyc
.venv/
cache/*
!cache/expfactory_stop_signal/
!cache/expfactory_stroop/
!cache/stopit_stop_signal/
!cache/cognitionrun_stroop/
/output/
dist/
*.egg-info/
.env
.env.local
.env.*.local
.worktrees/
archive/
.playwright-mcp/
scripts/*.log
# Trial-level Eisenberg reference data: too large for git (142 MB / 16 MB).
# Fetch per data/human/README.md; integrity via the sha256 sums recorded there.
data/human/stop_signal_eisenberg.csv
data/human/stroop_eisenberg.csv
.reasoner_work/
.reasoner-logs/
# Per-run validation artifacts. Production validation reports (under
# validation/ at top level) should be committed; smoke / debug subdirs
# are regeneratable and shouldn't churn git history.
output_smoke/
validation/smoke_*/
# SP11 Phase 5c variance study: work dirs (stage partials only — kept
# locally for reproducibility, regenerable). The variance-study output
# TaskCards under .variance_study/runN/expfactory_stroop/ ARE committed
# for audit.
.variance_study/work*/
.variance_study/*_pilot_run/
.variance_study/*_pilot_work/
