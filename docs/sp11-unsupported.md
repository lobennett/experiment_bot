# SP11 unsupported paradigms

Auto-generated by `experiment_bot.calibration.drop_from_scope.
append_unsupported_note`. Each entry corresponds to a paradigm
that failed the pilot-time alignment check during Phase 5b
regeneration after 2 retries (3 total attempts). These
paradigms are not measured in Phase 7 and are not part of
the SP11 cross-deployment claim.

## cognitionrun_stroop

- **Dropped at:** 2026-05-19T15:11:15-0700
- **Attempts:** 3
- **Reason:** Stage 6 pilot failed all 3 refinement attempts during Phase 7 smoke pilot validation. Each attempt hit a 300s hard timeout with the congruent condition never observed (incongruent matched 4501/4513 polls but congruent matched 0/4513). Phase detection 'complete' predicate never fires on cognition.run's experiment-end state, producing a runaway trial loop. The Phase 5b TaskCard (e62646a9.json) only appeared functional because --skip-pilot bypassed Stage 6; with pilot enforced (Phase 5c-Phase 7), the paradigm structurally fails. SP11 cross-deployment claim drops from 4 to 3 paradigms (expfactory_stroop, expfactory_stop_signal, stopit_stop_signal).



## RESOLVED 2026-05-20: cognitionrun_stroop returned to scope

The cognitionrun_stroop drop on 2026-05-19 was triggered by a Stage 6
pilot hang, root-caused on 2026-05-20 to a structurally underspecified
`navigation.phases` block in the Phase 5b regen TaskCard (the
single-`wait 1000ms` phase didn't actually fire the Space-keys needed
to advance cognition.run's multi-page instructions plugin).

Patch: replaced phases with `5 × (Space-keypress + 800ms wait)`. The
patched TaskCard runs end-to-end:

- 15 trials completed in 50s (matching the 4-page-instruction flow
  shown in real user data)
- Both `congruent` and `incongruent` selectors fire correctly
- Effective fidelity: **100% (15/15 paired × 15/15 within-pair)**
- Task complete detected on `#jspsych-data-display`

`task_specific.sp11_supported` flipped back to `True`. The cognitionrun
arm is measured in Phase 7 (queued after the running 3-paradigm sweep
finishes; see `docs/sp11-phase7-deliverable.md`).

This is a *navigation-phase-completeness* lesson worth surfacing for
Phase 8: Stage 6 pilot's hang signature was identical to a TaskCard
that crashes in the wild, so the Stage 6 hard-timeout treatment
(infer "structural failure", drop from scope) is correct but the
underlying defect was a Stage 1 navigation-phase blind spot — Stage 1
emitted a single `wait` instead of inferring the multi-page Space
advance from cognition.run's instructions plugin. Worth backlogging
as a Stage 1 prompt improvement for the next pipeline iteration.
